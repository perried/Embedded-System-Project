PORT=3001
INGEST_API_KEY=telco-guard

# PostgreSQL (matches docker-compose.yml defaults)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=telcoguard
DB_USER=telcoguard
DB_PASSWORD=telcoguard2026

# Firebase (for push notifications — optional, leave commented to disable)
# Option A: Path to JSON file (mount into container)
# FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-key.json
# Option B: Raw JSON string — paste your service account JSON on one line
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project",...}
